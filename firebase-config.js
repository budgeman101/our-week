/*
 * Firebase configuration.
 *
 * While this is empty, the app runs in LOCAL MODE: tasks are saved only in
 * the browser you're using, and nothing syncs.
 *
 * To turn on shared syncing, follow the "Turn on syncing" steps in README.md,
 * then paste your project's config values here. (These values are safe to
 * publish — access is controlled by your Firestore security rules, not by
 * keeping this file secret.)
 */
const FIREBASE_CONFIG = {
  apiKey: "",
  authDomain: "",
  projectId: "",
  storageBucket: "",
  messagingSenderId: "",
  appId: ""
};
